import React from 'react';
import { Layout } from "antd";

const {Content} = Layout;

export const Contacts = () => {
    return (
        <div>
            <Content>
                Users
            </Content>
        </div>
    );
};

